fn sum(a, b) {
    sum = a + b;
    return sum;
}

fn main() {
    var0 = 1;
    var1 = 1 + 2 + 3;
    var2 = 1 + 2 * 3;
    var3 = 1 + 2 * 3 + var2;
    var4 = var3 * (var0 + 4 * (2 + 8 * (3 - 2)));
    var5 = sum(1, sum(var0, 3));
}
